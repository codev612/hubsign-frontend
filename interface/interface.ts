export interface credential {
    email: string;
    password: string;
}

export interface UserAvatarProps {
    username?: string;
    email?: string;
};

export interface PageTitleBarProps {
    pageTitle: string,
    buttonTitle: string,
}